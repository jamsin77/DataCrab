# 前端按钮布局优化说明

## ✅ 已优化页面

### 1. 技能页面

**工具栏布局：**
```
调整前：
[上传] [生成] [分类筛选] [搜索]  ← 所有按钮挤在一起

调整后：
[上传] [生成]              [分类筛选] [搜索]
← 左侧操作按钮              → 右侧筛选工具 →
```

**优化内容：**
- ✅ 工具栏左右分离
- ✅ 左侧：操作按钮（上传、生成）
- ✅ 右侧：筛选工具（分类、搜索）
- ✅ 使用 `justify-content: space-between` 自动分布

---

### 2. 算子页面

**工具栏布局：**
```
调整前：
[上传] [生成] [分类筛选] [搜索]  ← 所有按钮挤在一起

调整后：
[上传] [生成]              [分类筛选] [搜索]
← 左侧操作按钮              → 右侧筛选工具 →
```

**优化内容：**
- ✅ 工具栏左右分离
- ✅ 左侧：操作按钮（上传、生成）
- ✅ 右侧：筛选工具（分类、搜索）

---

### 3. 调度页面

**工具栏布局：**
```
调整前：
[新建] [状态筛选] [任务类型]  ← 所有按钮挤在一起

调整后：
[新建]                     [状态筛选] [任务类型]
← 左侧操作按钮              → 右侧筛选工具 →
```

**优化内容：**
- ✅ 工具栏左右分离
- ✅ 左侧：操作按钮（新建调度）
- ✅ 右侧：筛选工具（状态、任务类型）

---

### 4. 数据源页面

**工具栏布局：**
```
调整前：
[新建] [类型筛选]  ← 所有按钮挤在一起

调整后：
[新建]                     [类型筛选]
← 左侧操作按钮              → 右侧筛选工具 →
```

**优化内容：**
- ✅ 工具栏左右分离
- ✅ 左侧：操作按钮（新建数据源）
- ✅ 右侧：筛选工具（类型筛选）

---

## 🎨 统一样式规范

### 工具栏CSS结构

```scss
.toolbar {
  display: flex;
  justify-content: space-between;  // 左右分布
  align-items: center;              // 垂直居中
  margin-bottom: 16px;
  gap: 12px;                        // 元素间距
  
  .toolbar-left {
    display: flex;
    gap: 12px;
    align-items: center;
  }
  
  .toolbar-right {
    display: flex;
    gap: 12px;
    align-items: center;
  }
}
```

### 布局原则

1. **左右分离**
   - 左侧：主要操作按钮（新建、上传、生成等）
   - 右侧：筛选和搜索工具

2. **间距统一**
   - 按钮间距：12px
   - 工具栏下边距：16px

3. **宽度优化**
   - 下拉选择框：120-140px
   - 搜索输入框：220px

4. **对齐方式**
   - 垂直居中对齐
   - 水平两端分布

---

## 📊 优化效果

### 调整前

```
┌────────────────────────────────────────┐
│ [按钮1] [按钮2] [筛选1] [筛选2] [搜索] │  ← 拥在一起
└────────────────────────────────────────┘
```

### 调整后

```
┌────────────────────────────────────────┐
│ [按钮1] [按钮2]        [筛选1] [筛选2] │  ← 整齐分布
└────────────────────────────────────────┘
```

---

## 🎯 其他优化

### 按钮组对齐

**卡片内按钮：**
```html
<div class="skill-actions">
  <el-button>修改</el-button>
  <el-button>调试</el-button>
  <el-button>下载</el-button>
  <el-button>转为流程</el-button>
  <el-button type="danger">删除</el-button>
</div>
```

**表格操作列：**
```html
<el-button size="small">测试</el-button>
<el-button size="small">浏览</el-button>
<el-button size="small">修改</el-button>
<el-button size="small">删除</el-button>
```

---

## 📝 修改文件列表

1. `frontend/src/views/skill/SkillView.vue`
   - 工具栏布局优化
   - CSS样式更新

2. `frontend/src/views/operator/OperatorView.vue`
   - 工具栏布局优化
   - CSS样式更新

3. `frontend/src/views/schedule/ScheduleView.vue`
   - 工具栏布局优化
   - CSS样式更新

4. `frontend/src/views/datasource/DataSourceView.vue`
   - 工具栏布局优化
   - CSS样式更新

---

## 🔄 刷新页面

**请按 `Ctrl + Shift + R` 硬刷新浏览器查看效果！**

---

## 📌 设计原则

1. **一致性**：所有页面使用相同的布局模式
2. **清晰性**：操作和筛选功能明确分离
3. **美观性**：整齐排列，间距统一
4. **易用性**：符合用户习惯，操作在左，筛选在右