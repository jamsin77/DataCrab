# 调度功能实现说明

## ✅ 已实现功能

### 后端功能

1. **数据模型** (`backend/app/models/schedule.py`)
   - Schedule: 调度配置模型
     - 支持任务类型: workflow, operator, skill
     - 支持调度方式: cron, interval, manual
     - Cron表达式配置
     - 固定间隔配置
     - 执行配置(重试、超时、并发)
   - TaskExecution: 任务执行记录
     - 执行状态跟踪
     - 执行结果和日志
     - 血缘关系记录

2. **API接口** (`backend/app/api/v1/endpoints/schedule.py`)
   - POST `/schedules` - 创建调度
   - GET `/schedules` - 获取调度列表
   - GET `/schedules/{id}` - 获取调度详情
   - PUT `/schedules/{id}` - 更新调度
   - DELETE `/schedules/{id}` - 删除调度
   - POST `/schedules/{id}/pause` - 暂停调度
   - POST `/schedules/{id}/resume` - 恢复调度
   - POST `/schedules/{id}/trigger` - 手动触发
   - GET `/schedules/{id}/executions` - 执行历史
   - GET `/schedules/executions/{id}` - 执行详情
   - POST `/schedules/validate-cron` - 验证Cron表达式
   - GET `/schedules/stats/overview` - 统计概览

### 前端功能

1. **统计面板**
   - 总调度数
   - 运行中/已暂停数量
   - 今日执行数
   - 成功/失败统计

2. **调度列表**
   - 名称和描述
   - 任务类型(算子/技能/工作流)
   - 调度方式(Cron/间隔/手动)
   - 状态管理
   - 下次执行时间
   - 上次执行状态

3. **创建/编辑对话框**
   - 基本信息(名称、描述)
   - 任务配置(类型、目标、参数)
   - 调度配置
     - Cron表达式 + 助手
     - 固定间隔
     - 手动触发
   - 执行配置(重试、超时、并发)

4. **Cron助手**
   - 常用预设(每分钟、每小时、每天等)
   - 示例说明
   - 实时验证

5. **执行历史**
   - 执行状态
   - 触发方式
   - 开始/结束时间
   - 耗时统计
   - 重试次数
   - 详细日志

## 📋 使用说明

### 创建调度

1. 点击"新建调度"按钮
2. 填写基本信息:
   - 调度名称: 例如"每日数据同步"
   - 描述: 调度说明

3. 选择任务:
   - 任务类型: 算子/技能/工作流
   - 选择具体的任务
   - 执行参数(JSON格式)

4. 配置调度:
   - Cron表达式: 例如 `0 8 * * *` (每天8点)
   - 或固定间隔: 例如每1小时
   - 或手动触发

5. 设置执行参数:
   - 最大重试次数
   - 重试间隔
   - 超时时间
   - 并发数

### 管理调度

- **暂停**: 暂停运行中的调度
- **恢复**: 恢复已暂停的调度
- **执行**: 立即手动触发一次
- **历史**: 查看执行历史
- **编辑**: 修改调度配置
- **删除**: 删除调度

### Cron表达式示例

| 表达式 | 说明 |
|--------|------|
| `*/5 * * * *` | 每5分钟 |
| `0 * * * *` | 每小时整点 |
| `0 8 * * *` | 每天8:00 |
| `0 9-17 * * *` | 每天9-17点整点 |
| `0 8 * * 1-5` | 工作日8:00 |
| `0 0 1 * *` | 每月1号零点 |

## 🔄 调度流程

```
创建调度
    ↓
计算下次执行时间
    ↓
调度器监控(待实现)
    ↓
到达执行时间
    ↓
创建执行记录
    ↓
执行任务(算子/技能/工作流)
    ↓
记录执行结果
    ↓
更新调度状态
    ↓
计算下次执行时间
```

## ⚠️ 待实现功能

1. **调度器引擎**
   - 后台调度监控线程
   - 定时触发任务执行
   - 并发控制
   - 失败重试

2. **任务执行器**
   - 算子执行
   - 技能执行
   - 工作流执行
   - 结果收集

3. **通知功能**
   - 执行成功通知
   - 执行失败告警
   - 邮件/钉钉/企微通知

4. **高级功能**
   - 依赖调度(A完成后执行B)
   - 条件触发
   - 数据质量检查
   - 血缘分析

## 🧪 测试步骤

1. 启动后端服务
2. 启动前端服务
3. 进入调度页面
4. 创建一个测试调度
5. 手动触发执行
6. 查看执行历史

## 📝 数据库迁移

需要运行数据库迁移以创建新表:

```bash
cd backend
alembic revision --autogenerate -m "update schedule model"
alembic upgrade head
```

或者直接使用SQLite(开发环境自动创建)。